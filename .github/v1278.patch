--- a/index.html
+++ b/index.html
@@ -24 +24 @@
-<meta content="v1277 · approved-audio-mock-match" name="fc-build"/>
+<meta content="v1278 · compact-live-audio-swipe-fix" name="fc-build"/>
@@ -26 +26 @@
-<meta content="Rebuilt Home/Compete audio entry cards to match the approved premium mock: exact near-square artwork/metrics proportions, sharp cinematic Crowned F artwork, integrated waveform treatment, modern dark play/pause control, real duration display, and stable swipe behavior." name="fc-whatsnew"/>
+<meta content="Corrected the premium audio player: compact 16:9 geometry without stretching, truly live waveform animation, single time display, reliable swipe-to-next with playback stop, and the same premium treatment on winner cards." name="fc-whatsnew"/>
@@ -8258,3 +8258,4 @@
-/* v1277 — measured from the approved audio mock. The player panel is 783×809:
-   783×683 artwork + 126px metric rail. Values below are percentages derived
-   from those measurements so the layout scales without changing composition. */
+/* v1278 — corrected implementation of the approved audio mock.
+   The near-square v1277 shell stretched the baked reference artwork and made
+   cards taller than winner media. Audio now shares a compact 16:9 stage, the
+   artwork is cropped rather than distorted, and only live UI is drawn over it. */
@@ -8272 +8273 @@
-  aspect-ratio:783 / 809!important;
+  aspect-ratio:16 / 9!important;
@@ -8280,2 +8281 @@
-  display:grid!important;
-  grid-template-rows:683fr 126fr!important;
+  display:block!important;
@@ -8282,0 +8283 @@
+  aspect-ratio:16 / 9!important;
@@ -8289 +8290 @@
-  touch-action:pan-x!important;
+  touch-action:pan-y!important;
@@ -8301,2 +8302,2 @@
-  background-position:center!important;
-  background-size:100% 100%!important;
+  background-position:center top!important;
+  background-size:100% auto!important;
@@ -8305 +8306,12 @@
-/* Real audio energy rides over the approved waveform without replacing it. */
+/* The reference art contains old static player pixels below the emblem. The
+   lower mask keeps the sharp cinematic art while clearing that UI so the live
+   play control, waveform and times are the only controls users see. */
+.fc-audio-premium-art-v1277::after{
+  content:""!important;
+  position:absolute!important;
+  left:0!important;right:0!important;bottom:0!important;top:60%!important;
+  z-index:3!important;
+  pointer-events:none!important;
+  background:linear-gradient(180deg,rgba(5,3,2,.18) 0%,rgba(5,3,2,.78) 28%,rgba(5,3,2,.97) 100%)!important;
+}
+/* Real audio energy replaces the old static EQ in the visible lower band. */
@@ -8308,4 +8320,4 @@
-  left:4.2%!important;
-  right:4.2%!important;
-  top:64.3%!important;
-  height:24.6%!important;
+  left:5.5%!important;
+  right:5.5%!important;
+  top:77%!important;
+  height:15%!important;
@@ -8316 +8328 @@
-  gap:1.15%!important;
+  gap:.8%!important;
@@ -8322 +8334,6 @@
-.fc-audio-premium-v1277.playing .fc-audio-premium-live-v1277{opacity:.45!important}
+.fc-audio-premium-v1277.playing .fc-audio-premium-live-v1277{opacity:.96!important}
+.fc-audio-premium-live-v1277::before{
+  content:""!important;position:absolute!important;left:0!important;right:0!important;top:50%!important;height:1px!important;
+  background:linear-gradient(90deg,transparent,rgba(255,158,38,.55) 12%,rgba(255,203,98,.92) 50%,rgba(255,158,38,.55) 88%,transparent)!important;
+  box-shadow:0 0 10px rgba(255,126,20,.45)!important;
+}
@@ -8336 +8353 @@
-  top:58.6%!important;
+  top:68%!important;
@@ -8338 +8355 @@
-  width:22.7%!important;
+  width:14.5%!important;
@@ -8341,4 +8358,4 @@
-  min-width:64px!important;
-  min-height:64px!important;
-  max-width:96px!important;
-  max-height:96px!important;
+  min-width:58px!important;
+  min-height:58px!important;
+  max-width:82px!important;
+  max-height:82px!important;
@@ -8370,12 +8387,2 @@
-/* Paused state uses the approved artwork's button pixels verbatim. The real
-   button is a transparent hit target until playback begins; on play it becomes
-   the matching dark pause control, covering the baked play triangle. */
-.fc-audio-premium-v1277:not(.playing) .fcc-audio-btn{
-  background:transparent!important;
-  border-color:transparent!important;
-  box-shadow:none!important;
-  backdrop-filter:none!important;
-  -webkit-backdrop-filter:none!important;
-}
-.fc-audio-premium-v1277:not(.playing) .fcc-audio-btn::before{opacity:0!important}
-.fc-audio-premium-v1277.playing .fcc-audio-btn::before{opacity:1!important}
+/* v1278: the live control is always opaque so no baked reference control can
+   leak through or double-render. */
@@ -8387 +8394 @@
-  bottom:3.8%!important;
+  bottom:4.2%!important;
@@ -8392,2 +8399,2 @@
-  min-width:11.7%!important;
-  height:7.4%!important;
+  min-width:10.5%!important;
+  height:12%!important;
@@ -8400 +8407 @@
-  font-size:clamp(12px,3.6vw,16px)!important;
+  font-size:clamp(11px,3.2vw,14px)!important;
@@ -8408,2 +8415,2 @@
-.fc-audio-premium-current-v1277{left:3.2%!important}
-.fc-audio-premium-total-v1277{right:3.2%!important}
+.fc-audio-premium-current-v1277{left:3.5%!important}
+.fc-audio-premium-total-v1277{right:3.5%!important}
@@ -8413 +8420 @@
-  display:grid!important;
+  display:none!important;
@@ -18556 +18563 @@
-  if (mode === 'card') {
+  if (mode === 'card' || mode === 'winner') {
@@ -18564 +18571 @@
-    const premiumRootActionV1277 = `onclick="toggleFccAudio(this,event)" onpointerdown="event.stopPropagation()"`;
+    const premiumRootActionV1277 = '';
@@ -18838 +18845 @@
-          bars[i].style.height = (16 + v * 78) + '%';
+          bars[i].style.setProperty('height', (16 + v * 78) + '%', 'important');
@@ -18876 +18883 @@
-            bars[i].style.height = (16 + envelope * pulse * 72) + '%';
+            bars[i].style.setProperty('height', (16 + envelope * pulse * 72) + '%', 'important');
@@ -18912 +18919 @@
-  tile.querySelectorAll('.fc-audio-premium-bar-v1277').forEach(b => { b.style.height = ''; });
+  tile.querySelectorAll('.fc-audio-premium-bar-v1277').forEach(b => { b.style.removeProperty('height'); });
@@ -32366,0 +32374,10 @@
+// v1278 — changing entries must end playback from the card being left.
+// This is deliberately media-generic so audio and deliberate inline video never
+// continue from an off-screen entry after a horizontal swipe.
+function fcPauseEntryCardMediaV1278(card) {
+  if (!card) return;
+  card.querySelectorAll('audio,video').forEach(media => {
+    try { if (!media.paused) media.pause(); } catch (_) {}
+  });
+}
+
@@ -32410,0 +32428 @@
+      cards().forEach((card, index) => { if (index !== targetIndex) fcPauseEntryCardMediaV1278(card); });
@@ -32459,0 +32478 @@
+        fcPauseEntryCardMediaV1278(cards()[g.index]);
@@ -32493,0 +32513 @@
+    fcPauseEntryCardMediaV1278(cards()[g.index]);
@@ -32548,0 +32569,4 @@
+      if (sc._fcActiveEntryIndexV1278 !== i) {
+        sc.querySelectorAll('.fcc-entry-card').forEach((card, n) => { if (n !== i) fcPauseEntryCardMediaV1278(card); });
+        sc._fcActiveEntryIndexV1278 = i;
+      }
@@ -40604,0 +40629,13 @@
+
+/* v1278 — winning audio uses the same compact premium player as live entries. */
+.fc-winner-media-v1058:has(.fc-winner-audio-v1123){
+  aspect-ratio:16 / 9!important;
+  max-height:none!important;
+  display:block!important;
+  background:#080503!important;
+}
+.fc-winner-audio-v1123,.fc-winner-audio-v1123 .fc-audio-premium-v1277{
+  width:100%!important;
+  max-height:none!important;
+  aspect-ratio:16 / 9!important;
+}
@@ -49780,0 +49818,2 @@
+
+<script>window.FC_V1278_VALIDATION={build:"v1278",compactAudio169:true,liveEqInlineImportantFix:true,swipeStopsPlayback:true,swipeSurfaceUnblocked:true,singleTimePair:true,winnerAudioPremium:true,updateParserPreserved:true};</script>
