itle}" was removed by moderation for violating community guidelines.`
-      : `🚫 Your entry to "${title}" was rejected by moderation and is not visible to others. Review the community guidelines before resubmitting.`;
+      : `🚫 Your entry to "${title}" was not approved. It is not visible to others. Open the challenge and replace the video before entries close to resubmit.`;
     await db.from('notifications').insert({
       user_id: e.user_id, type: 'entry_rejected', challenge_id: e.challenge_id,
       message, created_at: new Date().toISOString(),
@@ -33856,8 +33999,12 @@
     const entered = emberHasEntered(ch);
     const voted = state.emberVotedChallengeIds instanceof Set && state.emberVotedChallengeIds.has(ch.id);
     const myEntry = emberEntryFor(ch);
+    const myModerationV1288 = String(myEntry?.moderation_status || '').toLowerCase();
 
     if(status === 'submitting'){
+      if(myModerationV1288 === 'rejected') return { label:'Replace entry', relation:'Your video was not approved. Replace it before entries close to resubmit.', run:()=>{ state.currentChallenge=ch; openSubmitEntry(myEntry); } };
+      if(myModerationV1288 === 'pending') return { label:'Entry under review', relation:'Your video is uploaded, saved, and private until the safety check finishes.', run:()=>{ state.currentChallenge=ch; openSubmitEntry(myEntry); } };
+      if(myModerationV1288 === 'flagged' || myModerationV1288 === 'escalated') return { label:'Review in progress', relation:'Your video is saved and private while a closer review is completed.', run:()=>{ state.currentChallenge=ch; openSubmitEntry(myEntry); } };
       // v1275 — Championship entry is invitation-only on Home too. The old
       // Ember Home path predated canEnterChampionship() and could advertise a
       // generic Enter challenge action to non-qualified viewers.
@@ -34126,7 +34273,11 @@
   function fcAction(ch,status){
     const mine=!!state.profile?.id && String(ch.creator_id || ch.creator?.id || ch.user_id)===String(state.profile.id);
     const entry=fcMyEntry(ch);
+    const moderationV1288=String(entry?.moderation_status||'').toLowerCase();
     if(status==='submitting'){
+      if(moderationV1288==='rejected') return {label:'Replace entry',run:()=>{state.currentChallenge=ch;openSubmitEntry(entry);}};
+      if(moderationV1288==='pending') return {label:'Entry under review',run:()=>{state.currentChallenge=ch;openSubmitEntry(entry);}};
+      if(moderationV1288==='flagged'||moderationV1288==='escalated') return {label:'Review in progress',run:()=>{state.currentChallenge=ch;openSubmitEntry(entry);}};
       // v1275 — the Home shelf had its own older entry logic and bypassed the
       // authoritative Championship invite gate. Keep regular challenge behavior,
       // but never expose a generic Championship entry action to non-qualifiers.
@@ -34994,24 +35145,28 @@
       : ch.sd_phase === 'voting'
         ? approvedEntries.filter(e => Number(e.sd_round || 0) === sdRound)
         : approvedEntries;
-    const myEntry = myId ? allEntries.find(e =>
+    const myEntryAnyV1288 = myId ? allEntries.find(e =>
       String(e.user_id) === String(myId) &&
-      String(e.moderation_status || '').toLowerCase() !== 'rejected' &&
       inCurrentRound(e)) : null;
+    const myModerationAnyV1288 = String(myEntryAnyV1288?.moderation_status || '').toLowerCase();
+    const myRejectedEntryV1288 = myModerationAnyV1288 === 'rejected' ? myEntryAnyV1288 : null;
+    const myEntry = myRejectedEntryV1288 ? null : myEntryAnyV1288;
     const myModeration = String(myEntry?.moderation_status || '').toLowerCase();
     const hasPrivatePendingEntry = !!myEntry && MOD_PENDING.has(myModeration);
     const enteredThisRound = !!myEntry;
+    const hasOwnRowV1288 = !!myEntryAnyV1288;
     const challengeFull = allEntries.length >= MAX_ENTRIES_PER_CHALLENGE;
     const amTiedPlayer = isSdSubmitting && Array.isArray(ch.sd_user_ids) && ch.sd_user_ids.includes(myId);
-    const canEnter = !!state.profile && !enteredThisRound && (
+    const canEnter = !!state.profile && !hasOwnRowV1288 && (
       (status === 'submitting' && !challengeFull) ||
       (isSdSubmitting && amTiedPlayer)
     ) && canEnterChampionship(ch);
     const promotionBlocked = allEntries.some(e => MOD_PENDING.has(String(e.moderation_status || '').toLowerCase()));
     return {
       ch, status, allEntries, approvedEntries, visibleEntries, myId, myEntry,
+      myEntryAnyV1288, myRejectedEntryV1288, myModerationAnyV1288,
       myModeration, hasPrivatePendingEntry, enteredThisRound,
-      userHasEntered:enteredThisRound, challengeFull, amTiedPlayer, canEnter,
+      userHasEntered:enteredThisRound, hasOwnRowV1288, challengeFull, amTiedPlayer, canEnter,
       isSdSubmitting, sdRound, promotionBlocked,
       publicEntryCount:visibleEntries.length
     };
@@ -40344,9 +40499,10 @@
   const esc=v=>typeof escapeHtml==='function'?escapeHtml(String(v??'')):String(v??'');
   function left(d){if(!d)return '';const ms=new Date(d)-Date.now();if(ms<=0)return 'Ending now';const m=Math.ceil(ms/60000);if(m<60)return `${m}m left`;const h=Math.floor(m/60),mm=m%60;if(h<48)return `${h}h${mm?` ${mm}m`:''} left`;const days=Math.ceil(h/24);return `${days}d left`;}
   function statusCopy(ch,status){
-    const shared=window.fcGetChallengeDetailStateV1078?.(ch);const entries=shared?.approvedEntries||(state.currentEntries||[]).filter(e=>fcEntryCompetesV1060(e));const me=state.profile?.id;const mine=shared?.myEntry||null;const mod=shared?.myModeration||String(mine?.moderation_status||'').toLowerCase();const pending=shared?.hasPrivatePendingEntry??['pending','flagged','escalated'].includes(mod);
+    const shared=window.fcGetChallengeDetailStateV1078?.(ch);const entries=shared?.approvedEntries||(state.currentEntries||[]).filter(e=>fcEntryCompetesV1060(e));const me=state.profile?.id;const mine=shared?.myEntry||null;const rejectedV1288=shared?.myRejectedEntryV1288||null;const mod=shared?.myModeration||String(mine?.moderation_status||'').toLowerCase();const pending=shared?.hasPrivatePendingEntry??['pending','flagged','escalated'].includes(mod);
     if(status==='submitting'){
-      if(pending)return {k:'Getting your entry ready',t:left(ch.submission_deadline),h:'Private for a moment',p:'Your upload is complete. We’re finishing a quick safety check and will publish it automatically when ready. You can still edit or withdraw before entries close.',primary:'edit',mine};
+      if(rejectedV1288)return {k:'Entry not approved',t:left(ch.submission_deadline),h:'Replace your video to resubmit',p:'Your entry is saved, but it is not public or competing. Choose a replacement video before entries close and it will go through review again.',primary:'replace',mine:rejectedV1288};
+      if(pending)return {k:'Video under review',t:left(ch.submission_deadline),h:'Your entry is saved',p:'Your upload is complete and private while the safety check finishes. It will publish automatically if approved. You can stil